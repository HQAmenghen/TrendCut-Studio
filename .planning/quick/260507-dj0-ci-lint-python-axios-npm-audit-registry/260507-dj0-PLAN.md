# Quick Task 260507-dj0: CI lint, Python tests, axios audit

## Goal

Close the engineering loop by making CI fail on lint errors, adding a runnable Python test entry, keeping frontend build in CI, upgrading axios, and forcing production dependency audit to use the official npm registry.

## Tasks

1. [x] Update JavaScript package scripts and CI workflows.
   - Add `test:py` and `audit:prod`.
   - Make local CI require Node tests, Python tests, frontend build, lint, and production audit.
   - Make GitHub Actions lint fail normally and add Python/audit jobs.

2. [x] Make Python unittest discovery runnable.
   - Avoid import-time `sys.stdout` wrapper replacement that closes unittest output streams.
   - Refresh stale Gemini Vertex API-key assertions to match the current implementation.

3. [x] Upgrade axios and verify gates.
   - Upgrade axios to the current fixed release.
   - Run Node tests, Python unittest, lint, frontend build, and production audit.

## Verification

- `npm run test:py`
- `npm run lint`
- `npm test -- --runInBand`
- `npm run build:front`
- `npm run audit:prod`
- `npm run ci`
