# Quick Task 260521: X Platform Publish Scaffold

## Goal

Prepare the publish center for X automatic publishing before real X credentials are available.

## Tasks

1. Add X API publishing backend
- Files: `server/services/publish/xApi.js`, `server/services/publish/platformRpa.js`, `server/services/publish/wechatRpa.js`, `server.js`
- Action: Implement X API v2 video upload, post creation, cancellation, retries, failure summaries, and dispatch from the existing publish execution path.
- Verify: Unit tests cover dispatch and core X publishing behavior.

2. Extend X configuration and validation
- Files: `server/services/publish/publishStore.config.js`, `server/services/publish/store.js`, `.env.example`
- Action: Store X as an account list with OAuth2 fields, preserve legacy values, mask secrets, and validate selected account credentials safely.
- Verify: Config tests cover legacy migration and account validation.

3. Expose X in publish center UI
- Files: `frontend/src/composables/usePublishCenter.js`, `frontend/src/components/PublishCenterWorkspace.vue`
- Action: Add X account management, editor account selection, auto-pilot platform selection, and direct-publish-only messaging.
- Verify: Frontend build parses successfully.
