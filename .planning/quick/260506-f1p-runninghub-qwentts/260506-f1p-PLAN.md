# Quick Task 260506-f1p: 精简 RunningHub 前端配置并锁定 QwenTTS 合成音频上传

## Goal

Remove operator-facing RunningHub internals from the frontend and make the RunningHub renderer explicitly upload the QwenTTS synthesized speech audio instead of the voice reference audio.

## Tasks

1. Add failing tests for RunningHub speech audio routing and frontend configuration surface.
2. Update renderer calls to use `speechAudioPath` for RunningHub uploads.
3. Remove unnecessary RunningHub inputs and payload fields from the frontend.
4. Verify with Jest, server lint, frontend build, and diff checks.
