# 数据库状态遗留清理

原则：只做有实际收益的统一改造，不为“纯粹统一”而改造。

优先级：
1. 实时进度/阶段变化更频繁写入 TaskStore，提高刷新/重启后任务面板可信度。
2. 磁盘扫描从主状态来源降级为 DB 索引修复/旧任务迁移兜底。
3. 发布、RPA、XAI 等长任务桥接到统一任务视图，保留各自业务 DB 作为领域数据源。
4. 不把视频、字幕、执行计划等大产物塞进数据库。
