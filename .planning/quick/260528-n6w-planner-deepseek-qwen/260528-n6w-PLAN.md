# Quick Task 260528-n6w: 接入数字人动作大模型 planner 并真实跑 DeepSeek/Qwen 测试

## Scope

- Add an optional LLM planner for avatar motion action selection.
- Preserve the local semantic planner as a fallback.
- Keep the timeline and final trim behavior deterministic.
- Run the real task once with DeepSeek and once with Qwen.

## Tasks

1. Extend `python/pipeline/avatar_motion_plan.py` with LLM planning, response validation, pacing guards, and fallback behavior.
2. Pass planner/provider/model options through `server/services/materialDriven/avatarMotion.js`.
3. Add focused tests and generate real DeepSeek/Qwen motion-source videos for manual RunningHub import.
