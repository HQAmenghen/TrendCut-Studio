# Quick Task 260528-o5l: 接入 RunningHub InfiniteTalk 三输入工作流

## Scope

- Version the tested RunningHub workflow file in `config/`.
- Make the existing RunningHub renderer use the tested three-input contract by default.
- Keep the current RunningHub client and avatar generation flow; avoid a duplicate integration path.
- Update focused tests and commit only this integration slice.

## Tasks

1. Add a small RunningHub default config for workflow id and node mappings.
2. Wire avatar generation, auto-start, task state, and renderer defaults to the shared mapping.
3. Save the tested workflow JSON and verify with existing RunningHub/avatar tests.
