# Quick Task 260528-n1n: 动作参考视频按音频时长裁尾

## Goal

Keep action components mostly intact while ensuring the final motion reference video does not exceed the speech audio duration.

## Tasks

1. Add final duration trimming to `avatar_motion_source_builder.py`, using the motion plan duration by default.
2. Preserve existing full-component behavior inside the sequence; only trim the final rendered video tail when needed.
3. Verify with unit tests and regenerate the real task semantic motion video for manual RunningHub testing.
