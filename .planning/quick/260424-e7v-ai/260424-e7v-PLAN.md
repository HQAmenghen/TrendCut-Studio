# Quick Task 260424-e7v: 新增 AI 剪辑与竖屏合成失效/久远任务自清理

## Goal

Add automatic cleanup for task-level leftovers:

- Material-driven AI editing projects under `projects/material_*`.
- Vertical synthesis jobs under `data/uploads/xai_vertical_queue/*`, `public/xai_vertical_queue/*`, and matching `TaskStore` rows.

## Plan

1. Add focused cleanup tests first for stale complete tasks, invalid incomplete tasks, and protected active/recent tasks.
2. Implement task-aware cleanup helpers in `server/core/cleanup.js` with configurable 7-day retention and invalid-task grace period.
3. Wire scheduler cleanup to pass `taskStore` and `verticalQueueService`, so vertical task records and directories are cleaned together.
4. Run focused Jest tests and lint for changed server files.

## Safety Rules

- Do not delete queued/running in-memory vertical jobs.
- Do not delete recent incomplete material projects within the invalid-task grace period.
- Delete whole task workspaces only when the task is old or incomplete beyond the grace period.
- Support dry-run mode without deleting files or task records.
