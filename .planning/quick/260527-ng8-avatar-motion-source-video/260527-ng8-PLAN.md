# Quick Task 260527-ng8: Avatar Motion Source Video

**Date:** 2026-05-27
**Status:** Complete

## Goal

Remove the unused JSON pose path and make avatar motion generation output one stitched source video from action `source.mp4` templates plus static idle image segments.

## Actions

- Replace the pose builder with a video-only motion source builder.
- Remove unused JSON pose templates from the action library.
- Update Node motion generation to call the motion-source builder and return `avatar_motion_source.mp4`.
- Keep renderer integration unchanged: the generated video path is still passed as the configured pose/video workflow input.
